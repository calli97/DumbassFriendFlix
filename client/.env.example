# When using Vite's proxy (default for dev), this can stay as /api/v1
# For production, point to the actual server URL
VITE_API_URL=/api/v1

# Backend URL used by Vite's dev proxy (only needed in development)
# Change this if your backend runs on a different host/port
VITE_BACKEND_URL=http://localhost:3000
